# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# runtime data — the box keeps its own SQLite DB; never overwrite live picks/scores on deploy
/data/*.db
/data/*.db-shm
/data/*.db-wal

# env files (but keep the committed template)
.env*
!deploy/.env.production.example

# typescript
*.tsbuildinfo
next-env.d.ts

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
